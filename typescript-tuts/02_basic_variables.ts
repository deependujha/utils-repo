let myString = 'Deependu'; // auto type inference (string)
let myNumber = 10; // auto type inference (number)
let myBoolean = true; // auto type inference (boolean)
let myFloat = 10.5; // auto type inference (number)

// let myStringAuto: string = 'Deependu'; // manual type declaration (string)

console.log( `myString: ${myString}` );
console.log(`length of myString is: ${myString.length}`);
console.log(`myNumber: ${myNumber}`);
console.log(`myBoolean: ${myBoolean}`);
console.log(`myFloat: ${myFloat}`);
